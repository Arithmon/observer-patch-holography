\paragraph{Certified observer checkpoints.}
The trajectory enclosure and the authenticated execution admit a direct
error comparison at their common declared action times. Let $z(t)\in
\mathbb R^{10}$ comprise the five original positions and velocities of the
exact charged solution, and let $d_j$ be the state decoded from the
retained observer events at $t_j=j/40$, $0\leq j\leq80$.

\begin{proposition}[Error of the decoded charged state]
\label{cor:whitney-charged-checkpoint}
For the supplied action and initial data of
Proposition~\ref{prop:whitney-charged-time-enclosure},
\[
 \max_{0\leq j\leq80}\|d_j-z(j/40)\|_\infty
 \leq 10^{-10}+\frac{21}{2^{51}}
 <1.0001\times10^{-10}.
\]
\end{proposition}
\begin{proof}
Write $s_j$ for the stored binary64 reference samples, interpreted as exact
dyadic rationals. Fresh interval verification proves
$\|s_j-z(j/40)\|_\infty\leq10^{-10}$. Independently replaying all
$1782$ events reconstructs each decoded coordinate from its actual read
versions, retained responses and feedback writes. Exact rational comparison
over all $810$ decoded coordinates gives
$\max_j\|d_j-s_j\|_\infty=21/2^{51}$. The triangle inequality proves
the first bound; integer cross-multiplication proves the second.
\end{proof}

The reproducible certificate and independent verifier are
\path{code/electromagnetism/runtime/whitney_charged_checkpoint_receipt.json}
and \path{code/electromagnetism/verify_whitney_charged_checkpoint.py}.
Verification recomputes both parent arguments; copied display frames or
cached numerical diagnostics cannot supply the decoded state. The bound
applies to decoded position/velocity checkpoints. Intermediate destructive
probes need not approximate a physical trajectory, and no continuous
observer-state, nonlinear field-readout, Jacobi-quadrature, laboratory-clock
or quantum-evolution error bound is asserted.
