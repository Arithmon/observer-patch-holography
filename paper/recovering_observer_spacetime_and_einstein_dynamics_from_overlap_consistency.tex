\documentclass[11pt]{article}
\input{tex_fragments/DERIVATION_PAPER_PREAMBLE.tex}

\title{Recovering Observer Spacetime and Einstein Dynamics\\
from Overlap Consistency}
\author{B.\ M\"uller \and Alexander Osika \and Mario Poneder \and Kai Xue
\and Peter Nguyen \and Maarten Antonie Visser \and David Matscheko
\and Brieuc de La Fourni\`ere}
\date{\OPHPaperReleaseDate}

\begin{document}
\maketitle
\OPHPaperReleaseBanner

\begin{center}
\small\textbf{Author affiliations:} Bernhard Mueller, Pragma Research Inc.; Alexander Osika, EtherWorks.
\end{center}

\begin{abstract}
Observer-Patch Holography describes finite patches that record shared
boundaries and repair disagreement. On a declared twelve-port branch,
normalized repair response gives a rank-three metric quotient. Signed records
and conservative seam currents are dense in its continuous Euclidean
completion. Conservative Fibonacci record populations, with complete reads
inside a shrinking metric radius and a declared model clock, have causal
orders and normalized counts converging to flat $1+3$ spacetime. Their
ordering fraction in interior causal diamonds tends to $1/10$.
Segment-covering estimates control the finite causal and volume errors.
For nonzero closed convex pointed displacement cones, operational boost
covariance selects Lorentz shape; accessible interval counts reconstruct
proper-time ratios.
A separate positive scalar action on source-metric populations has controlled
continuum detector readings. Modular covariance and a round celestial limit
give Lorentz-frame kinematics. Positive null translations and finite tomography
reconstruct stress tensors under locality, covariance and Ward hypotheses.
With same-family physical stress identification, fixed-cap entropy
stationarity, controlled small-ball and curvature limits, universal coupling,
a vacuum reference and an independent scale, the Einstein equation follows
up to a cosmological constant. Finite MaxEnt identities also determine the
fixed-screen capacity-transfer law and de Sitter shock sign.
\end{abstract}

\section*{Derivation map}

Normalized repair response gives one positive rank-three source Gram
quotient $E$. Signed cumulative records and conservative seam words embed
densely in its Euclidean completion. Its unit directions form $S^2$;
adjoining a scalar line gives $\mathbb R\oplus E$ with quadratic form
$t^2-\|x\|^2$. This connects the source metric to a four-dimensional
Lorentz carrier without selecting a physical event clock or interpreting
every algebraic direction as a measured signal.

The conditional implications on this common carrier are
\[
\begin{aligned}
\substack{\text{nonzero closed convex pointed cone}\\
\text{finite irreducible operational rotations}\\
\text{one continuous boost direction}}
&\Longrightarrow
\substack{\text{Lorentz displacement cone}\\
\text{up to time orientation}},\\[1mm]
\substack{\text{conservative Fibonacci populations}\\
\text{complete local reads and }\Delta=a/c}
&\Longrightarrow
\substack{\text{flat causal-order limit}\\
\text{raw count--volume convergence}},\\[1mm]
\substack{\text{controlled order/count limit}\\
\text{accessible interval records}\\
\text{one reference interval}}
&\Longrightarrow
\text{proper-time ratios from counts},\\[1mm]
\substack{\text{positive source-metric scalar action}\\
\text{controlled sampling and smooth data}}
&\Longrightarrow
\substack{\text{continuum detector bounds}\\
\text{and finite oscillator quantization}},\\[1mm]
\text{positive translations and finite null tomography}
&\Longrightarrow
\text{conditional conserved }\langle T_{ab}\rangle,\\[1mm]
\substack{\text{physical stress and universal coupling}\\
\text{entropy, small-ball and tensor-curvature limits}}
&\Longrightarrow
G_{ab}+\Lambda g_{ab}=8\pi G\langle T_{ab}\rangle .
\end{aligned}
\]
These implications have distinct hypotheses. In particular, operational
boost covariance acts on admissible histories and interventions; an
algebraic action on the celestial sphere does not establish it. The
finite proper carrier rotations and a continuous boost along one axis
suffice to select the closed convex pointed causal cone by
Theorem~\ref{thm:operational-cone-selection}. This theorem selects cone
shape under its covariance premise, not a one-step read stencil or field
equation.

The constructive event family uses actual conservative source records.
Put $\varphi=(1+\sqrt5)/2$ and $L=2/\sqrt{\varphi+2}$.
For consecutive Fibonacci numbers $q=F_n$ and $p=F_{n+1}$, with $n\geq4$, its $q^3$
spatial records have a source-metric assignment to equal-volume cells
with error $H_q=2\sqrt3L/q$. Each event reads every preceding-layer
record inside radius $a_q=L/\sqrt q$, including its own preceding
record, and the model layer duration is $\Delta_q=a_q/c$, with $c>0$.
Segment subdivision bounds the reachable radius between
$c(1-2h_q/a_q)T$ and $cT$. Since $h_q\leq H_q$ and $H_q/a_q\to0$,
these cones agree in the limit away from their null boundary.
Proposition~\ref{prop:golden-source-count-limit} proves normalized raw
counts converging to $dt\,d^3x$ volume. On fixed interior timelike
intervals the ordering fraction tends to $1/10$. These are analytic
limits; the finite path construction, the layered order, the flat reference
constants and the count-clock enclosure are formalized in Lean, and the
finite readings of the family and of its carrier realization are recorded in
Section~\ref{sec:causal-manifold-observations}.

The population, complete-neighbour read rule, model tick and choice of one
counted event per site and layer are supplied. Independent exact replay
checks conservative currents, Gram-metric neighbours, actual writer
versions, schedule changes and transmitted interventions at finite
cutoffs. It does not turn those four controls into an asymptotic
experiment or identify the tick with a matter clock. Under the same
count limit, the observer readout $(|I|/|J|)^{1/4}$ converges to the
proper-time ratio of two interior timelike diamonds. It reads accessible
ancestry and counts, with a reference interval fixing units; its decoder
needs no coordinate labels or oscillator. Theorem~\ref{thm:source-count-clock}
and Proposition~\ref{prop:source-count-clock-error} state its limit and
finite-error conditions.

Exact order embeddings form a separate finite interface. For a source
spatial readback and positive height scale, a parent-edge speed bound
maps authenticated ancestry into the future cone. Equal-height spatial
separation and strict spacelike separation of increasing-height
nonancestors give the converse, hence an injective order embedding.
The source height is the attained longest authenticated-parent-chain
length. These finite statements do not identify height with proper time,
or element number with volume. A causal-set faithful embedding also
requires the appropriate population density\cite{bombelli1987,surya2019}.
The controlled source-record construction instead proves vanishing cone
and count errors; it need not reflect null comparisons exactly at each
finite resolution. Neither route selects curved topology or the physical
interpretation of its readouts. Under the relevant continuum causality
hypotheses, causal order and volume determine conformal geometry and
scale\cite{hawkingkingmccarthy1976,malament1977}.

The minimal source-direction Einstein-shape theorem places supplied symmetric
geometry and stress fields on the same finite event type. It asks for balance
on nine fixed algebraic source directions, four discrete step maps,
Ward/Bianchi identities, and step connectedness. Exact tomography derives
all-null balance, the pointwise metric ambiguity, its constancy, and
\[
 \mathsf{geometry}_{ab}
   =\kappa\,\mathsf{stress}_{ab}+\Lambda\eta_{ab}.
\]
The all-null relation is therefore a conclusion rather than a premise. The
minimal package contains no normalization, universal-source,
vacuum-calibration, or Newton-constant field. The nine-set
is algebraic, not selected by provenance or invariant as a set under Lorentz
transformations. No provenance edge selects a direction, tensor field, step,
or balance. The ambient carrier dimension and signature are constructed
independently by the source-causal carrier theorem; the poset dimension,
fields, and their physical interpretation remain separate.

Identifying \(\mathsf{geometry}\) with smooth curvature and
\(\mathsf{stress}\) with physical stress-energy requires the source-causal
continuum, count--volume, stress, small-ball, coupling, scale, and remainder
receipts on one family, together with either a tensor-curvature reconstruction
converging to the Einstein tensor or the independent continuum
small-ball/null-balance identification. Scalar-curvature convergence alone is
diagnostic. The full
\texttt{composedEinsteinBranch} remains available for that conditional
physical composition; it does not construct its source-derived tower.

Stress reconstruction consumes same-source null-stress matching,
locality/covariance, and the weak Ward balance; finite null tomography
does not supply them. The celestial \(S^2\) and the frame hyperboloid do not by
themselves populate a spacetime. The source-causal continuum and Einstein conclusions
are conditional theorems on their named receipts. Of the local-stress
inputs, one finite precursor is in hand at one energy component: a
face-local discrete Poynting law on the committed twelve-port carrier, exact
at every step of the scaled Maxwell evolution, whose face sum is the
committed global energy balance and whose density--flux pair is one
conserved current in the source-free case
(\path{Lean/Screen/LocalEnergyBalance.lean}). It supplies no momentum or
stress components, covariance, or continuum limit, and the identification
of the screen field energy with the modular energy the composition consumes
is read against one declared clause: the clock-and-energy calibration row
of the four-law surface, whose reference is the Gibbs state of a declared
energy at one declared inverse temperature \(\beta\). That row is inhabited
by the screen field energy, and against it the modular energy ledger of any
state is exactly \(\beta\) times its field-energy expectation plus
\(\log Z\), the cap first law takes the Clausius form
\(S(p)-S(\tau)=\beta(\langle H\rangle_p-\langle H\rangle_\tau)-D(p\Vert\tau)\)
with the offset cancelled, the slope and the reference are determined by
the ledger, the energy is free exactly up to an additive constant, and
\(\beta\) is determined by the reference once the family and the step are
declared; a two-point reference outside the Gibbs form has equal energy
expectations with unequal ledgers, so the affine reading is specific to
that clause. Nothing derives that the record reference is Gibbs and nothing
selects \(\beta\); the identification stays conditional on the declared
row (\path{Lean/Thermodynamics/GibbsReferenceEnergyIdentification.lean}).
The collar modular surface does not supply that inverse temperature. Its
one thermal object, the reference on the realized branch of the declared
maximum-entropy constraint, with modular Hamiltonian \(K=\lambda\cdot S\)
on the four joint sectors, has spectral
weights equal to the Gibbs state of \(K\) at \(\beta=1\) in modular units,
and for every nonzero \(\beta\) they equal the Gibbs state of \(K/\beta\)
at \(\beta\), so every temperature declaration inhabits the calibration row
for the collar reference and two declarations with different \(\beta\)
both do; a declared energy with two distinct levels pins \(\beta\), a
declared \(\beta\) pins the energy up to a constant, and the collar
reference is Gibbs for an energy \(H\) at \(\beta\) exactly when
\(K=\beta H+\mathrm{const}\). The collar functional is KMS at parameter one
for the inner flow of \(K\) and at parameter \(\beta\) for the flow of
\(K/\beta\), so the KMS parameter and the inverse temperature trade
against the energy scale. The identification of the collar weights with
the record reference of the cap first law stays declared
(\path{Lean/Thermodynamics/CollarTemperatureReading.lean}).

The sourced field equations on the committed complex are consistent in the
following exact sense. The hopping load and hopping current of a declared
port path satisfy the committed continuity equation at every port and every
step, and a finite family of hopping charges satisfies it with total load
equal to the sum of the charges. Along the scaled Amp\`ere evolution with any
current, the Gauss constraint propagates from one step to the next exactly
when the sources satisfy the continuity equation, so a field history that
solves the Amp\`ere evolution with a family of hopping charges and starts from
Gauss-consistent data stays Gauss-consistent at every step. The complete
criterion is an equivalence: for declared sources, a field history solving
the Amp\`ere evolution and the Gauss constraint at every step exists if and
only if the initial load is neutral and the sources are conserved; the
existence direction is an explicit second-order recursion in the temporal
gauge started from the canonical Coulomb field of the initial load. Because
the port boundary conserves total load on the closed carrier, one hopping
charge alone has a Gauss-consistent history only when its charge is zero; a
join of a worldline to the field sector therefore carries a neutral partner
or a neutralising background. A neutral pair of crossing charges, with
integer Green potential checked against the committed Green and incidence
tables by a kernel check, inhabits the scaled Maxwell bundle with a nonzero
current, and the unit crossing of every seam satisfies the continuity
equation by a kernel check that fixes the sign convention between the
crossing current and the incidence operator
(\path{Lean/Screen/SeamChargeContinuity.lean}).  The neutral-partner route is
also realized in one finite action rather than by appending an independently
prescribed background after variation.  For $h\ne0$, the source-free
Maxwell window plus the two opposite minimal couplings and two clock actions
is invariant under endpoint-vanishing gauge functions.  With the
seam-potential endpoints fixed, its field variations are equivalent to the
interior sourced Amp\`ere equations at $m<N$ and Gauss equations at
$n<N+1$.  Replacing either path over a closed two-step interval while
fixing the fields and other path gives an exact off-shell action difference,
not by itself a path equation of motion.  At $q=1$, $h=1/2$, the explicit
Coulomb-started packet is stationary in the field slots, but for every
$N\ge1$ delaying either its positive seam-$0$ crossing or its negative
seam-$29$ crossing, while fixing the fields and the other path, changes the
same action by $5/12$ for every pair of clock-unit values within the fixed
quadratic clock-action form.  The charge-conjugate results therefore fail the
declared closed-two-step exchange-stationarity condition in both path slots;
applying both canonical exchanges simultaneously changes the action by
$5/6$.  These are only the displayed discrete exchanges, not continuum
first-variation claims or a no-go for other paths, fields, or action shapes.
The counterexample is specific to that Coulomb start.  On the window $N=1$,
a second exact rational temporal-gauge history with the same crossings solves
the sourced equations and leaves the same action unchanged under every
in-window closed two-step replacement of either path.  The exhaustive route
check has two zero-difference endpoint routes and two triangular detours whose
clock difference $-4$ is cancelled by interaction difference $+4$.  The
sources are nonzero and opposite.  At the separately declared carrier unit
$\tau=3$, both crossings are timelike; identifying $\tau=h$ would instead make
a crossing spacelike
(\path{Lean/Screen/NeutralPairCoupledAction.lean},
\path{Lean/Screen/NeutralPairJointStationaryWitness.lean}).  The step, charges,
paths, rational history, clock unit, and action shapes are declared; the sign
of the committed load relative to a physical charge, source selection,
boundedness of the sourced history, a minimum or stability theorem, laboratory
calibration, and any continuum limit are outside this statement.

The three-dimensional source metric has an exact local construction. For the
declared twelve-port repair mean, the centered equal-port response kernel has
an exact spectral decomposition. Dividing by its slow response before taking
the infinite-step limit selects a rank-three projector. Its range in the
twelve-port counting space, equipped with the induced Gram form, is an
intrinsic Euclidean carrier. On the conditional signed cumulative
port-record/load module, the limiting form has a three-dimensional radical on
the real six-control extension. The integer module has no nonzero metric
kernel, embeds densely in the resulting quotient, and completes to the same
abstract continuous carrier, up to the unique isometry that preserves the
port labels. The source load map is exactly onto \(\mathbb Z^6\), with the
antipodally equal loads as its kernel. Conservative seam boundaries have
image
\[
 D_6=\{z\in\mathbb Z^6:\textstyle\sum_i z_i\equiv0\pmod 2\},
\]
with Smith invariants \((1,1,1,1,1,2)\). The pullback response metric makes
this submodule dense in the same completion; the ordinary six-dimensional
lattice metric on \(D_6\) is a different metric and is not used. Cumulative
record addition acts simply transitively on the exact record carrier and by
isometries on its completion. The sixty directed seam labels form one proper
carrier-rotation orbit and give the corresponding homogeneous internal
convolution under the stated naturality and unique-minimizer premises.
The thirteen finite covariance/isometry table identities in the proper-action
subpacket are checked with Lean's \texttt{native\_decide}; they consequently
trust the native compiler and runtime as well as the kernel and are not
kernel-only receipts.

Every finite-step kernel has rank eleven on the centered space and rank six
on the signed sector. The limiting response form supplies the rank-three
metric and the completion topology. The rotation-covariant seam average
selects a spatial operator under the stated feasibility and minimization
hypotheses. A physical propagation law additionally requires the population,
accepted dynamical moves and clock attachment. The source metric alone
does not choose a preferred Cartesian frame or identify record addition
with physical translation.

The Lorentz arrow is a composition with two independent inputs on one
refinement tower: the finite cap-normal Bisognano--Wichmann certificate,
whose clauses are cap-normal density and nondegeneracy, BW framing,
support-order faithfulness, geometric support-flow group and continuity
control, held-out oriented cross-ratio convergence, independently normalized
geometric \(2\pi\)-KMS comparison, and wrong-normalization separation, and an
independently produced complete multiresolution algebra-state comparison
package on the same tower. Geometry does not manufacture the modular state,
and bare finite consensus implies neither input. The Einstein arrow consumes
one common scaling family, universal coupling, a vacuum reference, and
independent scale readouts.

The finite boundary-circle diagnostic uses half-filled ground states of
anti-periodic nearest-neighbour hopping with Fermi velocity one, on rings
of $16,32,64,128$ sites. Summing every fitting odd hopping range of the
half-ring modular Hamiltonian gives median relative profile errors
$0.004808,0.001152,0.0002850,0.00007106$ against the conformal profile.
A fixed range-nine extraction instead increases from $0.0001124$ at $64$
sites to $0.0003381$ at $128$. Both extractions use the same Hamiltonian and
state. The omitted contribution at each bond is bounded algebraically by
$2\sum_{r>9}r|h_{i,i+r}|$ over fitting odd ranges, with
$i=j+(1-r)/2$ at bond $j$; its evaluation here is numerical.
These profile and transport cross-ratio comparisons establish
finite diagnostics, not a KMS identity on a common cap algebra or a
Cauchy-limit certificate
(\path{code/geometry/modular_clock_instrumentation.py}).

The formal soldering layer separates event readback from frame choice.
Once a coincidence quotient and invariant raw readback are supplied, the
readback descends uniquely. A time-oriented affine Lorentz overlap cocycle
and a compatible chart-coordinate family make Lorentz intervals chart
independent, future-null directions covariant, and observer rest readbacks
isometric under transport. The coordinate family must be identified
with the quotient-descended readback; the source Gram quotient supplies
its candidate spatial metric rather than a physical localization map.

Clock comparisons have a corresponding conditional interface. On a
unit-timelike affine history, positive clock increments equal dimensionless
proper time. Two ordered common events fix the positive-affine relation
between two declared clock scales; a third gives a cross-multiplication
check with no new fit parameter. These identities do not construct the
clock histories or attach their rates to the source dynamics. The count
clock provides a distinct, retrospective duration readout on the declared
source-record family, with explicit resolution requirements when durations
are accumulated along a moving worldline.

A kinematic mass-shell packet is machine-checked on the declared Hermitian
Lorentz module (\texttt{Lean/Geometry/MassShellKinematics.lean}). For one
declared mass parameter \(m\) and any future unit-timelike frame \(u\), the
scaled vector \(p = m\,u\) has Lorentz square exactly \(m^{2}\), and its
scalar coordinate obeys \(p_{0}^{2} = m^{2} + \lvert\mathbf{p}\rvert^{2}\).
For positive \(m\) the scalar coordinate dominates \(m\), with equality
exactly when the spatial coordinates vanish, and at the standard frame it
equals \(m\). Every oriented Lorentz map carries the construction to the
transported frame with the same Lorentz square, so the shell identity holds
in every oriented chart, and a future null vector satisfies the zero-mass
identity \(v_{0}^{2} = \lvert\mathbf{v}\rvert^{2}\). The mass parameter is a
declared hypothesis on the declared module, stated in its dimensionless
convention with unit invariant speed. The packet identifies no coordinate
with a physical energy readout and makes no physical rest-energy claim: the
clock and energy calibration anchors and the observer-to-physical-spacetime
attachment are not supplied.

A separate finite-graph precursor makes the discrete inverse-square route
premise-complete.  For a supplied integer layering, steady source law, shell
equidistribution, and shell count $c n^2$ through a finite depth, exact
divergence bookkeeping gives outward flux per shell edge
$Q/(c n^2)$, with $n^2$ times that flux independent of the shell.  An
explicit three-vertex chain inhabits the premises.  The same formalization
also proves why the depth cutoff cannot be hidden: on a closed finite carrier,
a globally steady drained source has zero total load, while an $n^2$ shell
count imposed at every positive layer forces $c=0$.  These are finite
conditional identities, not an identification of layer with physical radius,
flux with gravity, or source with mass density.

\tableofcontents
\clearpage

\def\OPHSkipStandardModelChain{1}
\def\OPHEinsteinDerivationPaper{1}
\def\OPHSkipEmbeddedReferences{1}
\input{tex_fragments/PAPER.tex}
\appendix
\input{tex_fragments/DERIVATION_TECHNICAL_SUPPLEMENT_PORT.tex}
\input{tex_fragments/SOURCE_NET_CAUSAL_LIMIT.tex}
\input{tex_fragments/SOURCE_RECORD_PROTECTION.tex}
\input{tex_fragments/SOURCE_POPULATION_QUADRATURE.tex}
\input{tex_fragments/SOURCE_SEAM_PATH_TOMOGRAPHY.tex}
\input{tex_fragments/OPERATIONAL_CAUSAL_SELECTION.tex}
\input{tex_fragments/SOURCE_COUNT_CLOCK.tex}
\input{tex_fragments/CAUSAL_MANIFOLD_OBSERVATIONS.tex}
\input{tex_fragments/SOURCE_METRIC_SCALAR_CONTINUUM.tex}
\input{tex_fragments/SOURCE_COMMON_SCALAR_PACKET.tex}
\input{tex_fragments/SOURCE_SCALAR_TIME_REFINEMENT.tex}
\input{tex_fragments/SOURCE_SCALAR_EXECUTION.tex}
\input{tex_fragments/SOURCE_SCALAR_CLOCK.tex}
\input{tex_fragments/SOURCE_SCALAR_QUANTUM.tex}
\input{tex_fragments/SOURCE_SCALAR_CLOCK_QUANTUM.tex}
\input{tex_fragments/SOURCE_SCALAR_REGIONAL_TIME_SLICE.tex}
\input{tex_fragments/OBSERVERS_REFERENCES.tex}

\section*{AI Assistance Disclosure}
This research project used research-grade commercial models, including
Anthropic's Fable and OpenAI's GPT-5.6-Sol, for research support, software
development, editing, and synthesis. The authors are responsible for the
paper's claims, methods, and final text.

\end{document}
