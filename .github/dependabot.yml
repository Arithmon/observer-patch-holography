# Keeps the commit-SHA pins on third-party Actions current. Each update
# arrives as a pull request that changes the SHA and its version comment.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
