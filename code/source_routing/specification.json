{
  "schema": "oph.source_routing.path_tomography_spec.v1",
  "support": "support_w12_l3.json",
  "path_lengths": [1, 4, 12, 24],
  "variants": ["baseline", "source_plus_one", "nonsource_minus_one"],
  "preparation": "Path port r starts at 1000 + ((7919*r + 13) mod 100003). Unscheduled ports do not enter the episode.",
  "schedule": "For start=d-1,...,0, apply pair means to (start,start+1),...,(d-1,d); retain the destination value initially and after every phase.",
  "arithmetic": "Exact rational port states, authenticated versions and protected destination-local samples; no finite precision or physical bandwidth is inferred.",
  "interleaving": "No other seam operation touches the path during this declared episode.",
  "stopping_rule": "Execute every declared path and variant once; no adaptive extension.",
  "scope": "Finite destructive readout transport, not a full metric-neighbor routing refinement or a population/count-clock certificate."
}
