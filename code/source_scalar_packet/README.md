This package compares a declared massive free scalar action on prepared golden
source addresses with the Dirichlet continuum field. The candidate observer
interface uses bounded patches with protected address records, mutable local
field registers, local read ports and bounded smeared readback effects. Native
repair selection and authenticated field/count-clock execution are separate.

Reproduce from the repository root:

```sh
python3 code/source_scalar_packet/source_common_scalar.py --check
python3 code/source_scalar_packet/verify_source_common_scalar.py
python3 -m pytest -q code/source_scalar_packet/test_source_common_scalar.py
```

The producer uses outward rational intervals. The independent verifier checks
actual source coordinates, reconstructs edge fluxes and reverses tridiagonal
elimination. Tests check interval arithmetic, leakage, compact-support tails,
false clock claims and resealed scientific forgeries. The JSON contains only
the four declared refinements, exact enclosures and source custody.

The mathematical q=233 model has 232^3 oscillators; tensor factorization uses
one-dimensional arrays of length232. It is not a full observer-network run.
The Fock spaces retain all oscillator modes. The comparison isometry acts on
a finite continuum spectral subspace; compact-smearing tails are bounded
separately. Geometry axes, boundary, action, quantization, prepared address
memory and model time are declared. No charged interaction, physical clock,
empirical instrument or source-selected action is certified here.
